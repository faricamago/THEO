{
  "buildCommand": "cd frontend && npm run build",
  "installCommand": "npm install && npm install --workspace=frontend && npm install --workspace=api",
  "outputDirectory": "frontend/dist",
  "env": {
    "NODE_ENV": "production"
  },
  "functions": {
    "api/src/index.ts": {
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/src/index.ts"
    }
  ]
}
