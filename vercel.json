{
  "buildCommand": "cd frontend && npm run build",
  "installCommand": "npm install --audit=false && npm install --workspace=frontend --audit=false && npm install --workspace=api --audit=false",
  "outputDirectory": "frontend/dist",
  "env": {
    "NODE_ENV": "production"
  },
  "functions": {
    "api/src/index.ts": {
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/src/index.ts"
    }
  ]
}
